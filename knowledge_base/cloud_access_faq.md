# Cloud Access FAQ

AWS sandbox access is approved within one business day after the manager and cloud governance group approve the request.

Users who see repeated console sign-out loops should clear their identity provider cookies and retry the sign-in flow.

Root access is prohibited for normal work. Emergency access uses a break-glass workflow that requires security approval and an audit note.

Developer sandbox roles are read-write only inside the sandbox account and cannot modify production resources.
